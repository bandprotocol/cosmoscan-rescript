open CssJs

// TODO: refactor later
let unit = 4
let xs = px(unit)
let sm = px(unit * 2)
let md = px(unit * 4)
let lg = px(unit * 6)
let xl = px(unit * 8)
let xxl = px(unit * 10)
let xxxl = px(unit * 16)
