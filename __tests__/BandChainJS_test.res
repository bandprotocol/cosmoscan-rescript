open Jest
open BandChainJS
open Expect

describe("Expect BandChainJS Client Module binding work correctly", () => {
  test("should be able call getReferenceData and recieve data in correct structure", () =>
    expect(1)->toEqual(1))
})
